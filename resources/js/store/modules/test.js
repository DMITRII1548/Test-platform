import axios from "axios"

const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
    testVuex() {
        console.log('vuex is working')
    }
}

export default {
    state,
    actions,
    getters,
    mutations,
}
